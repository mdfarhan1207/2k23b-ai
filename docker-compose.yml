version: "3.9"

services:
  backend:
    build: ./backend
    container_name: ai-backend
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env
    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: ai-frontend
    ports:
      - "80:4173"
    depends_on:
      - backend
    restart: unless-stopped